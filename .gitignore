node_modules/
out/
dist/
*.vsix
.vscode-test/
.DS_Store

# Secrets (PAT etc.) — never commit these
.env
.env.*
!.env.example

# The integration fixture ships a hand-written node_modules on purpose
!test/fixtures/workspace/node_modules/

.private/
local.kiritanconfig

# `.agents/skills/` is the single canonical copy of each installed skill;
# every other agent-specific location is a generated symlink/copy — run
# `npx skills experimental_install` to recreate them, don't commit them.
.claude/skills/
agent/skills/
