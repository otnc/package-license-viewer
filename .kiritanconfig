import { defineConfig } from "kiritan";

export default defineConfig({
  locales: { default: "en", list: ["en", "ja"] },
  sources: [
    { glob: "i18n/README.base.md", strategy: "inline" },
    { glob: "i18n/CONTRIBUTING.base.md", strategy: "inline" },
  ],
  // {base}.md at the project root for the default locale (README.md, CONTRIBUTING.md), {base}.{locale}.md for every other locale (README.ja.md, CONTRIBUTING.ja.md).
  naming: { template: "{base}.{locale}.{ext}" },
  // The switcher goes right after the H1. Both base files keep their H1 outside the per-locale blocks (so it's one shared heading node the switcher can anchor after), with its per-locale text supplied through these variables.
  switcher: { position: "after-heading" },
  interpolation: {
    variables: {
      readmeTitle: { en: "Package License Viewer", ja: "Package License Viewer" },
      contributingTitle: { en: "Contributing", ja: "貢献について" },
    },
  },
});
