// The newer manifest format: dependencies are declared as `module@version`.
name = "plv/app"

version = "0.1.0"

license = "MIT"

import {
  "moonbitlang/x@0.4.10",
  "plv/unpacked@0.1.0",
  "plv/legacy@0.1.0",
}
